7. Array Operations - Insert, Delete and Display
#include <stdio.h>

int main() {
    int a[100], n, choice, pos, value, i;

    printf("Enter number of elements: ");
    scanf("%d", &n);

    printf("Enter elements:\n");
    for (i = 0; i < n; i++)
        scanf("%d", &a[i]);

    while (1) {
        printf("\n1. Insert\n2. Delete\n3. Display\n4. Exit\n");
        printf("Enter choice: ");
        scanf("%d", &choice);

        if (choice == 1) {
            printf("Enter position and value: ");
            scanf("%d %d", &pos, &value);

            if (pos < 1 || pos > n + 1) {
                printf("Invalid position\n");
                continue;
            }

            for (i = n; i >= pos; i--)
                a[i] = a[i - 1];

            a[pos - 1] = value;
            n++;

            printf("Element inserted successfully.\n");
        }
        else if (choice == 2) {
            printf("Enter position to delete: ");
            scanf("%d", &pos);

            if (pos < 1 || pos > n) {
                printf("Invalid position\n");
                continue;
            }

            for (i = pos - 1; i < n - 1; i++)
                a[i] = a[i + 1];

            n--;

            printf("Element deleted successfully.\n");
        }
        else if (choice == 3) {
            printf("Array: ");
            for (i = 0; i < n; i++)
                printf("%d ", a[i]);
            printf("\n");
        }
        else if (choice == 4) {
            break;
        }
        else {
            printf("Invalid choice\n");
        }
    }

    return 0;
}
INPUT/OUTPUT:
Enter number of elements: 4
Enter elements:
10 20 30 40

1. Insert
2. Delete
3. Display
4. Exit
Enter choice: 1
Enter position and value: 3 25
Element inserted successfully.

Enter choice: 3
Array: 10 20 25 30 40

Enter choice: 2
Enter position to delete: 2
Element deleted successfully.

Enter choice: 3
Array: 10 25 30 40

